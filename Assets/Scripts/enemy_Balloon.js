#pragma strict

var character : GameObject;

function Start () {

character.Find("Creature_Body(Main)");

}

function Update () {

//transform.position.y = Mathf.Lerp(transform.position.y, character.transform.position.y, 5 * Time.deltaTime);

}